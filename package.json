{
  "name": "node-scrapper",
  "version": "0.0.0",
  "description": "Non-blocking node.js scrapper",
  "repository": "git@github.com:jakwuh/node-scrapper.git",
  "main": "lib/index.js",
  "scripts": {
    "build": "babel -s inline --presets es2015,stage-0 -d lib src",
    "start": "babel -s inline --presets es2015,stage-0 -d lib -w src",
    "prepublish": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "jakwuh <jakwuh@yandex-team.ru>",
  "license": "ISC",
  "publishConfig": {
    "registry": "https://registry.npmjs.org"
  },
  "dependencies": {
    "chalk": "^1.1.3",
    "co": "^4.6.0",
    "webdriverio": "^4.2.12"
  },
  "devDependencies": {
    "babel-cli": "^6.14.0",
    "babel-preset-es2015": "^6.14.0",
    "babel-preset-stage-0": "^6.5.0"
  }
}
